Make the service and genre screen act such that answers do not get submitted until all pages are finished.(store answers locally until step 3 is complete, then push to db)

Create homepage with filler movies until recommendation api is finished.

Implement the email verification as well as password reset

Implement the JWT for sign in

Update File Structure on readMe
